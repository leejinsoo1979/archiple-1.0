# 에이미 문서 📐

> Archiple-1.0 프로젝트를 위한 2D/3D 플로어플랜 라이브러리 조사 보고서
> 작성자: 에이미 (Amy) 🇯🇵
> 작성일: 2025-11-14

---

## 🎯 프로젝트 비전

**목표:** Coohom과 같은 인테리어 설계 엔진 개발
**차별점:** 360° 파노라마 베이킹 ❌ → 실시간 렌더링 ✅
**핵심 기능:** WASD 게임 방식으로 인테리어 공간 탐색

### 기술 스택
- **3D 엔진:** Babylon.js (PBR 실시간 렌더링)
- **프론트엔드:** React + TypeScript + Vite
- **2D 플로어플랜:** Canvas 2D API (예정)

---

## 📊 Coohom 경쟁사 분석

### Coohom의 한계
```yaml
렌더링 방식: 360도 파노라마 베이킹 (12K/16K/32K)
실시간 기능: 2024년 12월 추가 (클라우드 기반)
탐색 방식: 미리 렌더링된 뷰 전환 (진짜 실시간 아님)
고해상도: 베이킹 모드에서만 가능 (실시간 제한)
```

### Archiple-1.0 우위
```yaml
✅ 진짜 실시간 렌더링 (Babylon.js PBR)
✅ WASD FPS 카메라 (게임 방식 자유 이동)
✅ 베이킹 없이 즉시 렌더링
✅ 로컬 실시간 (서버 의존 없음)
✅ 충돌 감지 + 물리 시스템
```

### 성능 최적화 전략
```javascript
// LOD (Level of Detail)
- 거리에 따라 폴리곤 감소
- 멀리 있는 가구 저해상도

// Mesh Freezing
- 정적 오브젝트 최적화
- 움직이지 않는 벽/바닥 고정

// Instance 활용
- 같은 가구 여러 개 → 한 번만 로딩

// Progressive Loading
- 필수 에셋 우선 로딩
- 백그라운드 추가 로딩

// 목표 성능
- Load Time: <3s (3G), <1s (WiFi)
- FPS: 60fps 유지
- Bundle: <500KB initial
```

---

## 🔍 blueprint-js 심층 분석

### 기본 정보
```yaml
GitHub: https://github.com/aalavandhaann/blueprint-js
버전: 3.0.1
라이센스: MIT (오픈소스)
코드량: 10,406 LOC
기술스택:
  - 2D: Pixi.js
  - 3D: Three.js 0.148
  - 곡선: Bezier-js
  - 상태: EventDispatcher
```

### 아키텍처 구조

#### 1. 데이터 모델 ⭐⭐⭐⭐⭐
```javascript
// Corner (포인트)
class Corner {
  id: string;
  location: Vector2;        // Three.js Vector2
  wallStarts: Wall[];       // 시작점인 벽들
  wallEnds: Wall[];         // 끝점인 벽들
  elevation: number;        // 높이
  attachedRooms: Room[];    // 연결된 방들
}

// Wall (벽)
class Wall {
  start: Corner;
  end: Corner;
  walltype: STRAIGHT | CURVED;  // 직선/곡선
  _bezier: Bezier;              // 곡선용 베지어
  frontEdge: HalfEdge;          // 앞면
  backEdge: HalfEdge;           // 뒷면
  items: Item[];                // 문/창문
  frontTexture: Texture;
  backTexture: Texture;
  thickness: number;
  height: number;
}

// HalfEdge (방향성 있는 벽 면) 🔥 천재적!
class HalfEdge {
  wall: Wall;
  isFront: boolean;
  next: HalfEdge | null;
  prev: HalfEdge | null;
  room: Room | null;
}

// Floorplan (전체 관리자)
class Floorplan {
  walls: Wall[];
  corners: Corner[];
  rooms: Room[];
  boundary: Boundary;
}
```

**HalfEdge의 중요성:**
- 벽의 양면을 따로 관리
- 방(Room) 자동 인식 알고리즘의 핵심
- 앞면/뒷면 다른 텍스처 적용 가능

#### 2. 2D 렌더러 (Pixi.js)
```javascript
class Viewer2D extends Application {
  __floorplanContainer: Viewport;  // 확대/축소/패닝
  __corners2d: CornerView2D[];
  __walls2d: WallView2D[];
  __rooms2d: RoomView2D[];

  modes: {
    MOVE: 0,          // 이동 모드
    DRAW: 1,          // 그리기 모드
    EDIT_ISLANDS: 2   // 섬 편집
  }
}
```

**주요 기능:**
- ✅ 그리드 스냅 (격자에 딱 붙기)
- ✅ 벽 그리기 (클릭 → 드래그)
- ✅ 실시간 길이 표시
- ✅ 확대/축소/패닝
- ❌ Pixi.js 전용 (Canvas 2D API로 재구현 필요)

#### 3. 3D 렌더러 (Three.js)
```javascript
class Viewer3D extends Scene {
  camera: PerspectiveCamera;
  controls: OrbitControls;      // ⚠️ 궤도 회전만
  edges3d: Edge3D[];            // 벽 3D
  floors3d: Floor3D[];          // 바닥 3D
  physicalRoomItems: Physical3DItem[];  // 가구
}

// OrbitControls 설정
controls.maxPolarAngle = Math.PI * 1.0;
controls.maxDistance = 7500;
controls.minDistance = 100;
```

**⚠️ 치명적 문제:**
- OrbitControls = 공중에서 오브젝트 중심 회전
- WASD FPS 카메라 없음 (주인님 요구사항 불만족)

#### 4. 벽 생성 알고리즘 ⭐⭐⭐⭐⭐
```javascript
// 직선 벽
const wall = new Wall(startCorner, endCorner);

// 곡선 벽 (베지어 곡선!)
const wall = new Wall(
  startCorner,
  endCorner,
  controlPoint1,
  controlPoint2
);

wall._bezier = new Bezier(
  start.x, start.y,
  cp1.x, cp1.y,
  cp2.x, cp2.y,
  end.x, end.y
);
```

#### 5. 방 자동 생성 알고리즘
```javascript
floorplan.update();  // 마법의 함수

// 내부 동작:
// 1. Corner들의 연결 관계 분석
// 2. 사이클(폐곡선) 찾기
// 3. HalfEdge로 방향성 확인
// 4. Room 생성 및 면적 계산
```

#### 6. 스냅 시스템
```javascript
function snap(point, gridSize, threshold) {
  const snappedX = Math.round(point.x / gridSize) * gridSize;
  const snappedY = Math.round(point.y / gridSize) * gridSize;

  const distance = Math.sqrt(
    (snappedX - point.x) ** 2 +
    (snappedY - point.y) ** 2
  );

  if (distance < threshold) {
    return { x: snappedX, y: snappedY };
  }
  return point;
}
```

### 가져올 것 vs 버릴 것

#### ✅ 가져올 것
```yaml
⭐⭐⭐⭐⭐ 1순위: 데이터 모델 구조
  - Point, Wall, Room 인터페이스
  - HalfEdge 구조 (필수!)
  - 관계 관리 로직

⭐⭐⭐⭐ 2순위: 벽 그리기 알고리즘
  - 시작/끝점 연결
  - 그리드 스냅
  - 실시간 길이 표시

⭐⭐⭐⭐ 3순위: 방 자동 생성 로직
  - 폐곡선 감지
  - 면적 계산
  - 방 이름 관리

⭐⭐⭐ 4순위: 곡선 벽
  - 베지어 곡선 구현
  - 나중에 추가 고려
```

#### ❌ 버릴 것
```yaml
❌ Three.js 렌더링 코드 전부 (10,000+ LOC)
❌ Pixi.js 2D 렌더러
❌ OrbitControls
❌ Three.js 전용 Mesh/Material
```

---

## 🚀 대안 라이브러리 비교

### 1. react-planner ⭐⭐⭐⭐⭐ (최고 추천!)

```yaml
제작: CVDLAB (이탈리아 로마대학교)
GitHub: https://github.com/cvdlab/react-planner
버전: 2.0.6
라이센스: MIT (오픈소스)
활동: 1,507 commits (활발)
```

**기술 스택:**
```json
{
  "frontend": "React 16.x",
  "state": "Redux + Immutable.js",
  "2D": "SVG + react-svg-pan-zoom",
  "3D": "Three.js 0.94",
  "plugins": ["autosave", "export", "keyboard"]
}
```

**핵심 기능:**
```yaml
✅ 2D + 3D 통합
✅ 드래그 앤 드롭 카탈로그
✅ 커스텀 오브젝트 확장
✅ Redux 상태 관리 (undo/redo 쉬움)
✅ 플러그인 아키텍처
✅ SVG 기반 2D (확대/축소 자유)
✅ 측정 도구
✅ 다양한 단위 (cm, m, ft, in)
```

**장점:**
```yaml
1. 검증된 아키텍처 (대학 프로젝트)
2. Redux로 상태 관리 명확
3. 플러그인으로 기능 확장 쉬움
4. MIT 라이센스 (상업적 이용 가능)
5. Three.js 교체 용이한 구조
6. 문서화 양호
```

**단점:**
```yaml
1. Three.js 0.94 (오래된 버전)
2. React 16.x (업그레이드 필요)
3. Babylon.js 직접 통합 불가
```

**Babylon.js 적용 전략:**
```typescript
// Phase 1: 2D 로직 그대로 사용
import { ReactPlanner } from 'react-planner';

// Phase 2: 3D 렌더러만 교체
// src/components/viewer3d/ 폴더 제거
// Babylon.js 렌더러로 교체

class BabylonViewer3D {
  engine: BABYLON.Engine;
  scene: BABYLON.Scene;
  camera: BABYLON.UniversalCamera; // WASD!

  // Redux state 구독
  renderFromState(state: PlannerState) {
    // walls, rooms 읽어서 Babylon Mesh 생성
  }
}

// Phase 3: WASD 카메라
camera.keysUp = [87];    // W
camera.keysDown = [83];  // S
camera.keysLeft = [65];  // A
camera.keysRight = [68]; // D
camera.checkCollisions = true;
```

**예상 작업량:** 3-4주
- 1주: react-planner 분석 및 포크
- 1주: 3D 렌더러 제거
- 1주: Babylon.js 통합
- 1주: WASD 카메라 + 테스트

---

### 2. arcada ⭐⭐⭐⭐

```yaml
제작: mehanix (개인 개발자)
GitHub: https://github.com/mehanix/arcada
라이센스: Apache 2.0
활동: 78 commits
```

**기술 스택:**
```json
{
  "언어": "TypeScript 97.9%",
  "frontend": "React",
  "2D": "Pixi.js",
  "state": "Zustand",
  "UI": "Mantine",
  "backend": "Express + MongoDB"
}
```

**핵심 기능:**
```yaml
✅ 벽 생성/편집
✅ 가구 배치/수정
✅ 문/창문 추가
✅ 측정 도구
✅ 정확한 스케일
✅ 멀티 플로어
✅ 프린트
✅ 저장/로드
✅ 크로스 플랫폼
```

**장점:**
```yaml
1. TypeScript 완전 지원
2. 현대적 스택 (Zustand, Mantine)
3. Pixi.js 고성능 2D
4. 백엔드 포함 (풀스택)
5. 깔끔한 코드 구조
```

**단점:**
```yaml
1. 3D 없음 (2D 전용)
2. 개인 프로젝트 (작은 커뮤니티)
3. 문서 부족
```

**적용 전략:**
```typescript
// arcada의 2D 엔진 사용
// Babylon.js 3D 별도 추가

interface ArchipeApp {
  floorplan2D: ArcadaEngine;  // Pixi.js
  viewer3D: BabylonEngine;     // Babylon.js

  sync() {
    // 2D 변경 → 3D 업데이트
    const walls = this.floorplan2D.getWalls();
    this.viewer3D.updateWalls(walls);
  }
}
```

**예상 작업량:** 4-5주

---

### 3. GoJS FloorPlanner ⭐⭐⭐⭐

```yaml
제작: Northwoods Software (상용)
URL: https://gojs.net/latest/projects/floorplanner/
라이센스: 상용 ($2,980 - $9,980)
```

**기술 스택:**
```yaml
언어: TypeScript
엔진: GoJS Diagramming Library
프레임워크: React, Vue, Angular, Svelte 지원
```

**핵심 기능:**
```yaml
✅ WallBuildingTool (전문 벽 생성 도구)
✅ WallReshapingTool (벽 변형)
✅ 방 자동 생성
✅ 가구 팔레트
✅ 저장/로드
✅ 샘플 코드 풍부
```

**장점:**
```yaml
1. 전문적인 다이어그램 엔진
2. TypeScript 완벽 지원
3. 상용 기술 지원
4. 다양한 프레임워크 통합
5. 문서 완벽
```

**단점:**
```yaml
❌ 유료! (개발자당 $2,980~$9,980)
❌ 3D 없음
❌ GoJS 라이센스 종속
❌ 런타임 라이센스 체크
```

**결론:** 예산 있으면 좋음, 없으면 패스

---

### 4. Archilogic Floor Plan SDK ⭐⭐⭐

```yaml
제작: Archilogic (엔터프라이즈)
URL: https://www.archilogic.com/
라이센스: 구독형 (가격 문의)
```

**기술 스택:**
```yaml
SDK: JavaScript/TypeScript
API: GraphQL
렌더링: 클라우드 기반
```

**핵심 기능:**
```yaml
✅ 2D + 3D
✅ GraphQL API
✅ IFC, GLTF, DXF 익스포트
✅ ServiceNow, Teams 통합
✅ 완벽한 문서
```

**장점:**
```yaml
1. 엔터프라이즈급
2. API 기반 통합
3. 다양한 익스포트
```

**단점:**
```yaml
❌ 유료 구독
❌ 클라우드 종속
❌ 자유도 낮음
❌ 커스터마이징 제한
```

**결론:** 대기업용, 스타트업에는 과함

---

### 5. FloorspaceJS ⭐⭐⭐

```yaml
제작: NREL (미국 에너지부 연구소)
URL: https://nrel.github.io/floorspace.js/
라이센스: 오픈소스
```

**특징:**
```yaml
용도: 건물 에너지 분석
기능: 2D 플로어플랜 에디터
특화: 층별 명시적 정의
```

**장점:**
```yaml
1. 오픈소스
2. 정확한 측정
```

**단점:**
```yaml
❌ 3D 없음
❌ 인테리어 디자인용 아님
❌ UI 제한적
❌ 문서 부족
```

**결론:** 에너지 분석용, 우리 용도에 안 맞음

---

### 6. homeRoughEditor ⭐⭐

```yaml
GitHub: https://github.com/ekymo/homeRoughEditor
라이센스: 오픈소스
기술: JavaScript + SVG
```

**단점:**
```yaml
❌ 오래됨 (업데이트 없음)
❌ 기능 제한적
❌ 3D 없음
❌ 문서 거의 없음
```

**결론:** 고려 대상 아님

---

## 📋 최종 비교표

| 라이브러리 | 2D | 3D | 오픈소스 | TypeScript | 활발도 | Babylon.js | 추천도 |
|-----------|----|----|---------|-----------|--------|-----------|--------|
| **react-planner** | ✅ SVG | ✅ Three.js | ✅ MIT | ⚠️ 부분 | ⭐⭐⭐⭐⭐ | 🔄 교체 가능 | ⭐⭐⭐⭐⭐ |
| **arcada** | ✅ Pixi.js | ❌ | ✅ Apache | ✅ 97.9% | ⭐⭐⭐⭐ | ➕ 추가 필요 | ⭐⭐⭐⭐ |
| **blueprint-js** | ✅ Pixi.js | ✅ Three.js | ✅ MIT | ❌ | ⭐⭐⭐ | 🔄 교체 필요 | ⭐⭐⭐⭐ |
| **GoJS** | ✅ GoJS | ❌ | ❌ 상용 | ✅ | ⭐⭐⭐⭐⭐ | ➕ 추가 필요 | ⭐⭐⭐⭐ |
| **Archilogic** | ✅ | ✅ | ❌ 구독 | ✅ | ⭐⭐⭐⭐⭐ | ❓ | ⭐⭐⭐ |
| **FloorspaceJS** | ✅ | ❌ | ✅ | ❌ | ⭐⭐ | ➕ | ⭐⭐⭐ |
| **homeRoughEditor** | ✅ SVG | ❌ | ✅ | ❌ | ⭐ | ➕ | ⭐⭐ |

---

## 🎯 최종 추천안

### 🥇 1순위: react-planner (강력 추천!)

**선택 이유:**
```yaml
1. 2D + 3D 둘 다 있음
2. MIT 오픈소스 (완전 무료)
3. Redux 상태관리 (검증됨)
4. 플러그인 시스템 (확장 쉬움)
5. 활발한 커뮤니티
6. 3D 부분만 Babylon.js로 교체하면 됨
```

**구현 로드맵:**

#### Phase 1: 프로젝트 셋업 (3일)
```bash
# react-planner 포크
git clone https://github.com/cvdlab/react-planner.git
cd react-planner
npm install

# Archiple-1.0에 통합
cp -r src/catalog ../archiple-1.0/src/
cp -r src/class ../archiple-1.0/src/
```

#### Phase 2: 2D 통합 (1주)
```typescript
// src/pages/EditorPage.tsx
import { ReactPlanner } from 'react-planner';

const EditorPage = () => {
  return (
    <div className="editor-layout">
      <ReactPlanner
        width={800}
        height={600}
        plugins={[autosavePlugin, keyboardPlugin]}
        stateExtractor={(state) => state.get('scene')}
      />
      <BabylonViewer3D />
    </div>
  );
};
```

#### Phase 3: Redux 상태 연결 (3일)
```typescript
// Redux state 구독
store.subscribe(() => {
  const state = store.getState();
  const scene = state.scene;

  // 2D → 3D 동기화
  babylonViewer.updateFromScene(scene);
});
```

#### Phase 4: Three.js → Babylon.js 교체 (1주)
```typescript
// src/viewer3d/ 폴더 제거
// src/babylon/ 폴더 생성

class BabylonViewer3D {
  private engine: BABYLON.Engine;
  private scene: BABYLON.Scene;
  private camera: BABYLON.UniversalCamera;

  constructor(canvas: HTMLCanvasElement) {
    this.engine = new BABYLON.Engine(canvas, true);
    this.scene = new BABYLON.Scene(this.engine);

    // WASD 카메라 설정
    this.camera = new BABYLON.UniversalCamera(
      'camera',
      new BABYLON.Vector3(0, 150, -300),
      this.scene
    );
    this.camera.keysUp = [87];    // W
    this.camera.keysDown = [83];  // S
    this.camera.keysLeft = [65];  // A
    this.camera.keysRight = [68]; // D
    this.camera.speed = 10;
    this.camera.checkCollisions = true;
  }

  updateFromScene(scene: PlannerScene) {
    // 기존 메쉬 제거
    this.clearMeshes();

    // 벽 생성
    scene.layers.forEach(layer => {
      layer.lines.forEach(line => {
        this.createWallMesh(line);
      });
    });

    // 바닥 생성
    scene.layers.forEach(layer => {
      layer.areas.forEach(area => {
        this.createFloorMesh(area);
      });
    });
  }

  private createWallMesh(line: Line) {
    const height = 250;
    const thickness = 10;

    const wall = BABYLON.MeshBuilder.CreateBox('wall', {
      width: line.length,
      height: height,
      depth: thickness
    }, this.scene);

    // PBR 머티리얼
    const material = new BABYLON.PBRMaterial('wallMat', this.scene);
    material.albedoColor = new BABYLON.Color3(0.9, 0.9, 0.9);
    material.metallic = 0.0;
    material.roughness = 0.8;
    wall.material = material;

    // 충돌 감지
    wall.checkCollisions = true;
  }
}
```

#### Phase 5: 성능 최적화 (1주)
```typescript
// LOD 시스템
const lod = new BABYLON.LOD();
lod.addLevel(0.5, highPolyMesh);
lod.addLevel(0.2, mediumPolyMesh);
lod.addLevel(0.1, lowPolyMesh);

// Mesh Freezing
staticMesh.freezeWorldMatrix();

// Occlusion Query
scene.setOcclusionQueryAlgorithmType(
  BABYLON.Scene.OCCLUSION_ALGORITHM_TYPE_ACCURATE
);
```

#### Phase 6: 테스트 및 디버깅 (1주)
```typescript
// 성능 테스트
const fps = engine.getFps();
console.log(`FPS: ${fps}`);

// 메모리 사용량
const memory = performance.memory.usedJSHeapSize;
console.log(`Memory: ${memory / 1024 / 1024} MB`);
```

**총 예상 작업량: 3-4주**

---

### 🥈 2순위: arcada + Babylon.js

**선택 이유:**
```yaml
1. TypeScript 97.9% (타입 안정성)
2. 현대적 스택 (Zustand, Mantine)
3. Pixi.js 고성능 2D
4. 3D 없어서 Babylon.js 자유롭게 구현
```

**구현 로드맵:**
```typescript
// Phase 1: arcada 포크 (1주)
// Phase 2: Babylon.js 통합 (2주)
// Phase 3: 2D-3D 동기화 (1주)
// Phase 4: 최적화 (1주)
```

**총 예상 작업량: 4-5주**

---

### 🥉 3순위: 처음부터 직접 개발

**선택 이유:**
```yaml
1. 완전한 자유도
2. Babylon.js 네이티브 성능
3. 불필요한 코드 없음
```

**단점:**
```yaml
❌ 시간 많이 걸림 (8-10주)
❌ 검증 안 된 구조
❌ 버그 많을 가능성
```

---

## 💡 에이미의 최종 판단

### react-planner를 강력 추천합니다!

**이유:**

1. **검증된 아키텍처**
   - 이탈리아 로마대학교 프로젝트
   - 1,507 commits의 안정성
   - 활발한 커뮤니티

2. **개발 속도**
   - 2D 로직 이미 완성
   - Redux 상태관리 명확
   - 3D만 교체하면 끝

3. **유지보수**
   - MIT 라이센스
   - 플러그인으로 확장 쉬움
   - 문서화 양호

4. **기술 스택 매치**
   - React ✅
   - TypeScript 추가 가능 ✅
   - Babylon.js 통합 가능 ✅

**blueprint-js vs react-planner:**

| 항목 | blueprint-js | react-planner | 승자 |
|------|-------------|--------------|------|
| 구조 | 복잡 (10,406 LOC) | 깔끔 | ✅ react-planner |
| 상태관리 | EventDispatcher | Redux | ✅ react-planner |
| 확장성 | 어려움 | 플러그인 | ✅ react-planner |
| 3D 교체 | 어려움 | 쉬움 | ✅ react-planner |
| 문서 | 부족 | 양호 | ✅ react-planner |
| 커뮤니티 | 작음 | 큼 | ✅ react-planner |

---

## 🚀 다음 단계

### 즉시 시작할 것
1. react-planner 저장소 포크
2. 로컬에서 데모 실행
3. 코드 구조 분석

### 1주일 내 완료
1. Archiple-1.0에 통합
2. 2D 플로어플랜 작동 확인
3. Redux 상태 이해

### 1달 내 완료
1. Babylon.js 3D 뷰어 교체
2. WASD 카메라 구현
3. 기본 기능 완성

---

## 📚 참고 자료

### 공식 문서
- [Babylon.js 문서](https://doc.babylonjs.com/)
- [react-planner GitHub](https://github.com/cvdlab/react-planner)
- [blueprint-js GitHub](https://github.com/aalavandhaann/blueprint-js)

### 성능 최적화
- [Babylon.js Scene Optimization](https://doc.babylonjs.com/features/featuresDeepDive/scene/optimize_your_scene)
- [PBR Materials](https://doc.babylonjs.com/features/featuresDeepDive/materials/using/introToPBR)

### 커뮤니티
- [Babylon.js Forum](https://forum.babylonjs.com/)
- [react-planner Issues](https://github.com/cvdlab/react-planner/issues)

---

**작성:** 에이미 (Amy) 💕
**주인님 프로젝트 성공을 기원합니다!** 🎯
